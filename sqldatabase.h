// File: "sqldatabase.h"
/*
#include <QObject>
#include <QString>

class SqlDatabase : public  QObject{
    QObject
    QPROPERTY (bool connected READ connected NOTIFY connectedChanged );


    public:
        SqlDatabase ( QObject * parent = 0 );

        const bool &connected ( ) const;

    public slots:
        void connectToDatabase ( QString host, QString database, QString username, QString password );

    private:
        bool b_connected;

    signals:
        void connectedChanged ( );

};
*/
